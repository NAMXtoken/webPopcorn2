VITE_OCR_PROVIDER=tesseract
# Leave as "tesseract" for free, on-device OCR. Swap to "mock" for demo mode or "googleVision" if you wire up the cloud endpoint.
# Optional OMDb override (defaults to shared demo key if unset)
# VITE_OMDB_KEY=trilogy
# VITE_GOOGLE_VISION_KEY=your-google-vision-api-key
# VITE_TMDB_KEY=your-tmdb-api-key
# VITE_OMDB_KEY=your-omdb-api-key
